<Window x:Class="Lopatkin_Glazki.ChangePriorityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lopatkin_Glazki"
        mc:Ignorable="d"
        Title="ChangePriorityWindow" Height="250" Width="300">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Максимальный приоритет:"/>
            <TextBox x:Name="MaxPriorityTextBox" IsReadOnly="True" Margin="0,0,0,10"/>
            <TextBlock Text="Новый приоритет:"/>
            <TextBox x:Name="NewPriorityTextBox" Margin="0,0,0,10"/>
            <Button Content="Сохранить" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
